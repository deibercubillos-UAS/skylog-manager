/**
 * COMPONENTE LEGACY — no se usa en producción.
 * El sidebar real con RBAC dinámico está integrado directamente en
 * src/app/dashboard/layout.js.
 *
 * Este archivo se conserva para evitar errores de importación si algún
 * componente externo lo referencia. No renderiza nada.
 */
export default function Sidebar() {
  return null;
}
